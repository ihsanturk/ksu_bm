#include <stdio.h>

int main()
{
	/* Pointerlar hafizada bir degiskeni belirtmek icin kullanilir.
	 * Bir pointerin degeri hafiza adresi seklindedir.
	 *
	 * Pointer kullanimi su sekildedir:
	 * 
	 * int x;
	 * int *p;
	 * p = &x; // "&x" demek x'in adresi demektir.
	 *
	 *
}
